#include "merge-sort.hh"

#include <algorithm>


/**

If the current container has only one element, it is sorted
Otherwise, split the container in half
Recursively sort both parts by calling merge_sort on them
Merge those two parts in the right order

**/

void merge_sort(iterator_type begin, iterator_type end)
{
    if (begin + 1 == end)
        return;
    else
    {
        
    }
}